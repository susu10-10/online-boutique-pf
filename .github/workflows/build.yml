name: Build and Push Boutique Containers

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  TRIVY_VERSION: 0.72.0
  DOCR: registry.digitalocean.com/idpreg

jobs:
  security-scan:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - name: Run Trivy IaC scan on deploy config
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: deploy/
          severity: HIGH,CRITICAL
          exit-code: "1"
          format: table

      - name: Lint Docker Compose file
        run: |
          docker compose -f deploy/docker-compose.yml config
          echo "✅ Docker Compose file is valid"

      - name: Lint all Dockerfiles
        uses: hadolint/hadolint-action@v3.3.0
        with:
          dockerfile: src/*/Dockerfile
          failure-threshold: error

  build-and-push:
    needs: security-scan
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DO_TOKEN }}
      - name: Login to DOCR
        run: doctl registry login
      - name: Build all images
        run: docker compose -f deploy/docker-compose.yml build

      - name: Scan images with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: image
          scan-ref: |
            registry.digitalocean.com/idpreg/frontend:latest
            registry.digitalocean.com/idpreg/checkoutservice:latest
          severity: HIGH,CRITICAL
          exit-code: "0"
          format: table
      - name: Push all images
        run: docker compose -f deploy/docker-compose.yml push